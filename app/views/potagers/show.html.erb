<div class="container final-connect">

  <div class="modelisation">
    <h1>Votre potager</h1>
    <%= cl_image_tag("terracognita/rendu-final", width: 400) %>
    <input type="button" value="Générer un PDF" onClick="window.print()" class="btn   btn-home btn-lg btn-final-transparent btn-pdf">
  </div>

  <div class="compte">
    <h2>Grâce à mon compte :</h2>
      <ul class="list-unstyled compte-list">
        <li><i class="fas fa-leaf"></i> Je peux modifier mes parcelles</li>
        <li><i class="fas fa-leaf"></i> J'optimise mon rendement grâce à des informations clés</li>
        <li><i class="fas fa-leaf"></i> Je retrouve ma parcelle d'année en année et bénéficie de conseils pour les rotations de culture</li>
      </ul>
      <%= link_to "Créer un compte", new_user_registration_path, class:"btn btn-home btn-lg btn-final" %>
  </div>



<% @potager.vegetables.each do |vegetable| %>
 <p><%= vegetable.name %></p>
<% end %>

<% @potager.vegetables.each do |vegetable| %>
 <p><%= vegetable.photo %></p>
<% end %>

<% @potager.parcels.each do |p| %>
  <p>Order :<%= p.order_from_south %></p>
  <p>Size :<%= p.size %></p>
<% end %>

<p>Vegp: <%= @veg_parcels %></p>

<table>
  <% @potager.parcels.each do |pv| %>
   <tr> X <% @potager.vegetables.each do |vegetable| %>
     <td><%= vegetable.name %></td>
    <% end %><tr>

  <% end %>
</table>


<table>
  <% @potager.parcels.each do |pv| %>
   <tr> <% @potager.vegetables.each do |vegetable| %>
     <%= vegetable.name %>
    <% end %> <br>

  <% end %>
</table>

------------ avec les index ----->
<table>
  <% @potager.parcels.each do |pv| %>
  <tr><td><% @potager.vegetables.each do |vegetable| %>
     <%= vegetable.name %>
    <% end %></td></tr>


  <% end %>
</table>

------------ avec les index ----->





<!------------ pas touch à la div en-dessous ----->

</div>




<!-- pour afficher la nav bar avec la carotte colorée
  <%= render 'shared/progressbar' %>
-->

